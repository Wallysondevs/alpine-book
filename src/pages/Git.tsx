import { PageContainer } from "@/components/layout/PageContainer";

export default function Git() {
  return (
    <PageContainer
      title="Git (Completo)"
      subtitle="Controle de vers\u00e3o no dia a dia, do init ao push."
      difficulty="iniciante"
      timeToRead="20 min"
    >
      <p>Este capítulo já está na trilha e será escrito em breve. A estrutura do curso está pronta — acompanhe as novidades!</p>
    </PageContainer>
  );
}
