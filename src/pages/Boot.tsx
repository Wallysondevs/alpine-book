import { PageContainer } from "@/components/layout/PageContainer";

export default function Boot() {
  return (
    <PageContainer
      title="Boot & Bootloader"
      subtitle="Kernel, initramfs (mkinitfs), GRUB e syslinux: como o Alpine liga."
      difficulty="avancado"
      timeToRead="20 min"
    >
      <p>Este capítulo já está na trilha e será escrito em breve. A estrutura do curso está pronta — acompanhe as novidades!</p>
    </PageContainer>
  );
}
