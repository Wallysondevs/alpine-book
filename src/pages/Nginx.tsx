import { PageContainer } from "@/components/layout/PageContainer";

export default function Nginx() {
  return (
    <PageContainer
      title="Nginx"
      subtitle="Servidor web r\u00e1pido com OpenRC, vhosts e TLS via acme.sh."
      difficulty="intermediario"
      timeToRead="20 min"
    >
      <p>Este capítulo já está na trilha e será escrito em breve. A estrutura do curso está pronta — acompanhe as novidades!</p>
    </PageContainer>
  );
}
